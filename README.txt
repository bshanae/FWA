FWA

Instrucctions:
    -Set imaget path folder on src/resources/application.properties
    -Set dataBase with table's schema on src/resources/sql/schema.sql
    -Create war file and set tomcat server